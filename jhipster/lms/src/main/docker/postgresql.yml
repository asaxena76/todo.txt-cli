version: '2'
services:
    lms-postgresql:
        image: postgres:9.6.2
        # volumes:
        #     - ~/volumes/jhipster/lms/postgresql/:/var/lib/postgresql/
        environment:
            - POSTGRES_USER=lms
            - POSTGRES_PASSWORD=
        ports:
            - 5432:5432
